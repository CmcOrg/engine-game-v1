server:
  port: 20000

spring:
  application:
    name: engine-game-v1
  cloud:
    nacos:
      server-addr: 121.36.195.98:8848
      config:
        namespace: game
        file-extension: yaml
        extension-configs:
          - data-id: engine-game-v1.yml
          - data-id: mysql-engine-game-v1.yml
          - data-id: refresh-engine-game-v1.yml
            refresh: true
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
